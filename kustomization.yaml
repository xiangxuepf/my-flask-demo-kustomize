apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - secret.yaml

images:
  - name: 364950776/flask-demo
    newTag: latest  # 可以通过 CI/CD 动态替换为 git SHA 或版本号
